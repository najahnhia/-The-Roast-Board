import React from "react";
import { createRoot } from "react-dom/client";
import RoastBoard from "../JSX.CODE";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RoastBoard />
  </React.StrictMode>,
);
